-- Example ScrapScript file for testing syntax highlighting
map double [1, 2, 3]
; double = n -> n * 2
; map = f -> 
    | [] -> []
    | h >+ t -> (f h) >+ (map f t)

-- More examples
user = { name = "Alice", age = 30 }
result = user.name ++ " is " ++ (user.age :: string)

-- Pattern matching with variants
status = #ok "success"
message = | #ok msg -> msg | #error err -> "Error: " ++ err